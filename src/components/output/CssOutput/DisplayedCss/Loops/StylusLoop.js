import React from 'react';
import RuleSet from '../RuleSet/Index';
import Declaration from '../RuleSet/Declaration';

const StylusLoop = (props) => {
    return (
        <div className="loop">
            This is a Stylus Loop
        </div>
    );
}

export default StylusLoop;
 