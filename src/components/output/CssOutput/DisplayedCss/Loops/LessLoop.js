import React from 'react';
import RuleSet from '../RuleSet/Index';
import Declaration from '../RuleSet/Declaration';

const SassLoop = (props) => {
        /*return (
            <div className="loop">
                <div>.generate-columns(4);</div>

                <div>
                    .generate-columns(
                    <span className="variable">@n</span>
                    ,&nbsp;
                    <span className="variable">@i</span>
                    <span className="colon">:</span>
                    &nbsp;
                    1) when (@i =< @n)"} <span className="bracket">{"{"}</span></div>
      
                <div className="bracket">{"}"}</div>
            </div>
        );*/
        return (
            <div className="loop">
                This is a LESS Loop
            </div>
        );
}

export default SassLoop;
 